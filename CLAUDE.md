# Undercut — F1 Dashboard

AI assistant context for this repository.

## Tech Stack

- **Framework:** Next.js 16 (App Router), React 19
- **Language:** TypeScript (strict)
- **Styling:** Tailwind CSS v4 with custom `f1-*` design tokens
- **Data fetching:** SWR with an OpenF1 API proxy at `/api/f1/[...endpoint]`
- **Charts:** Recharts
- **Animations:** Framer Motion
- **Font:** Geist Sans + Geist Mono
- **Testing:** Vitest + Testing Library
- **CI:** GitHub Actions (lint + build on push/PR to main)
- **Deployment:** Vercel

## Data Source

All F1 data comes from the [OpenF1 API](https://openf1.org). Requests are proxied through `/src/app/api/f1/[...endpoint]/route.ts` and cached to disk at `.cache/openf1/`.

Replay position snapshots are pre-processed server-side at `/api/f1-replay-positions` using `src/lib/f1/replay-snapshot.ts`, which normalises GPS coordinates to a `0–100` SVG viewBox and caches the result.

## Project Structure

```
src/
  app/                      # Next.js App Router pages
    api/
      f1/[...endpoint]/     # OpenF1 proxy with disk cache
      f1-replay-positions/  # Pre-processed replay snapshot endpoint
    calendar/               # Race calendar page
    live/                   # Session replay + timing (main feature)
    race/[sessionKey]/      # Post-race analysis (laps, pitstops, telemetry, etc.)
    standings/              # Driver + constructor standings
  components/
    cards/                  # StatCard, RaceCard, SessionCard, etc.
    charts/                 # Recharts wrappers (LapTime, GridVsFinish, etc.)
    layout/                 # Navbar
    live/                   # Timing board, replay map, replay controls, race control feed
    tables/                 # ResultsTable
    ui/                     # Primitive components (Button, Skeleton, Tabs, etc.)
  lib/
    f1/                     # Replay snapshot builder, constants (cancelled meetings)
    hooks/                  # SWR data hooks (use-laps, use-positions, etc.)
    openf1/                 # API client + TypeScript types
    swr-config.tsx          # Global SWR provider
    utils/                  # analytics, colors, formatters, replay-processor, cn
scripts/
  pipeline/                 # Data pipeline scripts (circuit geometry, etc.)
  generate-gps-circuits.mjs # Circuit SVG path generator from MultiViewer data
public/
  logo.svg                  # Hero logo (blue border + blue "cut")
  logo-nav.svg              # Navbar logo (white border + white "cut")
  logo.png                  # Source PNG
  sw.js                     # Service worker (PWA, caches static assets only)
  manifest.json             # PWA manifest
```

## Design Tokens

All colours are defined in `src/app/globals.css` as CSS custom properties and exposed via `@theme inline` for Tailwind:

| Token | Value | Use |
|---|---|---|
| `f1-bg` | `#050510` | Page background |
| `f1-surface` | `#0e0e14` | Card/panel backgrounds |
| `f1-card` | `#111118` | Elevated cards |
| `f1-border` | `#1a1a24` | Default borders |
| `f1-accent` | `#4f8fff` | Primary accent (blue) |
| `f1-red` | `#E10600` | F1 brand red (tire compounds only) |
| `f1-text` | `#f0f0f5` | Primary text |
| `f1-text-secondary` | `#9898a8` | Secondary text |
| `f1-text-muted` | `#5e5e70` | Muted/labels |

## Key Conventions

- **Client components** are marked `"use client"` — data fetching uses SWR hooks.
- **Server components** are the default — page-level components avoid `"use client"` unless necessary.
- **Replay state** lives in `src/lib/hooks/use-replay-state.ts` and is passed down as props.
- **Timing data** is built by `buildTimingData()` in `timing-board.tsx` — pit out laps are skipped, sector bests reset per qualifying phase.
- **Circuit paths** are pre-generated by the pipeline and stored in `src/lib/f1/circuits/` as JSON.
- **Cancelled meetings** are tracked in `src/lib/f1/constants.ts` as `CANCELLED_MEETING_KEYS`.
- Do not add docstrings or comments unless the logic is non-obvious.
- Prefer editing existing files over creating new ones.

## Development Workflow

```bash
npm run dev            # Start dev server
npm run build          # Production build
npm run lint           # ESLint
npm run test           # Vitest (unit tests)
npm run test:coverage  # Vitest with coverage (run before pushing a PR branch)
npm run pipeline       # Run data pipeline scripts
```

CI runs parallel `lint` and `test` jobs on every push; `build` is gated on both passing. Vercel auto-deploys on merge to `main`.

### Process

- **Issue first:** Always create a GitHub issue before starting a feature or bug fix. Implement only when told "implement issue #N".
- **Small fixes / style changes:** Push directly to `main` — no branch or PR needed.
- **Features and bugs:** Issue → branch (`feat/`, `fix/`, `style/`, `chore/`, `refactor/` prefix) → run `npm run test:coverage` → PR with `Closes #N` in the body → merge.

### Commit style

Use [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: add qualifying timer freeze on red flag
fix: remove duplicate Bearman entry in timing board
style: center numeric columns in timing board
chore: update CI to parallel lint/test jobs
refactor: extract buildPhase helper in replay-processor
```

- No `Co-Authored-By` lines — do not attribute commits to Claude.
- No scope parentheses required (e.g. `feat:` not `feat(live):`).

### Known data quirks (OpenF1)

- Some drivers have **no position data** in OpenF1 qualifying (e.g. STR, SAI in 2026 Australian GP). They participate but GPS telemetry is absent. Treat them as normal entrants — append to timing board at the bottom and knock them out of Q1 at the same time as other Q1 eliminations.
- OpenF1 sometimes returns **duplicate driver entries** for a session (e.g. two `#87 BEA` entries in 2026 Australian GP qualifying). Guard against duplicates when building driver lists.
